{
  "name": "caicara-site",
  "version": "1.0.0",
  "description": "Site institucional Caiçara Marketing Digital — assets e blocos GHL servidos via jsDelivr",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "render:og": "node scripts/render-og.mjs",
    "deploy:check": "node scripts/check-version.mjs"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
